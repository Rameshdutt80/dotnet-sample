image: microsoft/dotnet:latest

stages:
    - build
    - test

variables:
    test: "Example.Test"

before_script:
    - "cd src"
    - "dotnet restore"

build:
    stage: build
    script:
        - "dotnet build"
        - mkdir build
        - touch build/info.txt
    artifacts:
      paths:
        - build/

test:
    stage: test
    script:
        - echo "testing"
        - "cd $test"
        - "dotnet test"
        - test -f "build/info.txt"
